#include <stdio.h>

int main(){
    int n,flag=0;
    scanf("%d",&n);
    int arr[n];
    for(int i=0; i<n; i++)
    {
        scanf("%d",&arr[i]);
        
    }
    int x;
    scanf("%i",&x);
    for(int i=0; i<n; i++)
    {
        scanf("%i",&arr[i]);
        if(x==arr[i]){
           flag=1;
           break; 
        }
    }
    if(flag){
        printf("Found");
    }
    else{
        printf("Not Found");
    }
    return 0;
}
    
